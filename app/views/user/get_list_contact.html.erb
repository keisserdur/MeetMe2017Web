<h1>Lista de contactos</h1>
<p>Devuelve la lista de contactos que tenemos en nuestra agenda</p>

    <table border=1>
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>TLF</th>
            <th>Token</th>
            <th>Email</th>
            <th>photo</th>
            <th>status</th>
            <th>lastconexion</th>
            <th>privacity</th>
            <th>VER PERFIL</th>
        </tr>
        <% @userlist.each do |value| %>
            <tr>
                <td><%= value.id %></td>
                <td><%= value.name %></td>
                <td><%= value.tlf %></td>
                <td><%= value.token %></td>
                <td><%= value.email %></td>
                <td><%= value.photo %></td>
                <td><%= value.status %></td>
                <td><%= value.lastconexion %></td>
                <td><%= value.privacity %></td>
                <td><%= link_to "Ver", controller: "user", action: "get_user_info", id: value.id %></td>
            </tr>
        <% end %>
    </table>
    <br>
    <%= link_to "Home", controller: "index", action: "home" %>
    <br>
    <%= link_to "App", controller: "index", action: "app" %>

